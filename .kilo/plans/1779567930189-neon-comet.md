# NEON COMET — P31 Sovereign Syllabus: Project Runway Deployment, Storyline Alignment & ZOLANDERY 11/10 Amplification
**Date:** 2026-05-23  
**Operator:** Semantic Director (via Kilo)  
**Target:** 04_SOFTWARE/p31ca — the family-facing "really good-looking people" portal  
**Status:** Plan complete, ready for execution turn. No edits performed yet.

---

## DEEP DIVE FINDINGS (Wide Parallel Exploration — 2026-05-23)

A full monorepo sweep (p31ca/src, workers/, shared-components/onboarding/, e2e/, utils, phos-v2, dome/geodesic, AbdicationCeremony, passkey worker, PGLite singletons, qr-printable, GenesisFlow, useSpoonState, resinEngine, etc.) revealed three **real but fragmented** live crypto onboarding paths and one **unused but perfectly aligned** sovereign onboarding package:

1. **Passkey/WebAuthn (production, hardware-bound)** — `workers/passkey/src/index.ts` + `src/pages/connect.astro` (lines 429-541). Full register-begin → navigator.credentials.create → register-finish with CBOR/COSE/Subtle verify → D1 credentials + k4-personal anchor. Used for Node Zero pairing too. Not Ed25519 "device soul", not triggered from syllabus.

2. **Abdication Ceremony (theatrical Ed25519 + WYE→DELTA narrative)** — `src/components/AbdicationCeremony.astro` (471 LOC). 5-step (Awakening → Covenant → Forge with real @noble/ed25519 keygen + typewriter log + localStorage vault → Covenant signature → Complete). "Slide to Abdicate" tactile gesture. Already uses the exact language ("ropes are open", "Sanctuary", "The WYE extracts. The DELTA protects."). Not yet fused with the syllabus or Spoon Dial.

3. **CWP-020 Genesis Protocol (the missing link)** — `shared-components/onboarding/` (full React package, never imported into p31ca):
   - `GenesisFlow.tsx` + screens (Gateway → Abdication → IdentityForge → FamilyCovenant → Complete) with framer-motion.
   - Real Ed25519 via `@noble/curves/ed25519` in `utils/identityCrypto.ts`.
   - Real PGLite genesis schema (`db/001_genesis_schema.sql`): `sovereign_identity` (alias, pub, encrypted priv, role: genesis_pending → mesh_sovereign, genesis_hash), `genesis_covenants`, `sync_queue`.
   - `utils/pglite-identity.ts` + `resinEngine.ts` (Kintsugi economics).
   - `hooks/useSpoonState.ts` — **already integrates the exact 1/3/6 Spoon Dial with the Resin/LOVE engine** (declareSpoonState, tierFromUI, multiplier, liquid balance). This hook was built for P31Welcome but is unused in the main app.
   - `INTEGRATION-GUIDE.md` explicitly says "the Spoon Dial is the cognitive adapter for the ceremony".

**The Syllabus "VIP Pass"** (`SyllabusPortal.tsx:204-280` + `syllabus.ts:109-117`) is still 100 % static fake 7×7 CSS grid QR + hardcoded text. It *describes* the perfect unified ceremony the other pieces already implement in fragments.

**Creative Opportunity (Never Seen Before):** The high-fashion Spoon Dial (the "ultimate assignment" you gave) is not just a theme switch — it is the **perfect director for a live cryptographic + psychological ritual**. No one has ever built a cognitive-load-adaptive onboarding ceremony where tapping "1 Spoon" collapses the entire 5-phase Genesis into a single giant safe button that still produces a real Ed25519 + PGLite-hydrated sovereign identity, while "6 Spoon" gives you the full 3D Posner Molecule "calcium cage" visualization (reusing existing tetra/dome/PosnerLattice tech) with live matrix rain of your actual private-key bytes while the four nodes of the Delta light up in real time.

This is the ultimate fusion: the "really good-looking people" storybook (syllabus) becomes the actual runway they walk, and their current spoon level *physically changes the security theater itself*.

---

## THE CREATIVE VISION: THE DELTA RUNWAY IGNITION CEREMONY

**Route:** New top-level `/ignite` (or `/syllabus/ignite` as progressive enhancement from the existing "Initiate Ignition Sequence" button).

**UX Contract:**
- User arrives via the familiar syllabus (or direct link from a physical VIP envelope).
- They read the 9 modules in their current spoon mode (still fully ZOLANDERY).
- When they hit Vol 3 and tap the big button, the entire page **morphs** (not a jarring navigation) into the live 4–5 phase Genesis ritual, but the Spoon Dial **remains live in the top-right corner for the entire ceremony**.
- Changing spoon mid-ritual instantly re-renders the current phase in the appropriate aesthetic and cognitive load:
  - **1 Spoon (Survival Couture):** Brutalist white, 64 px text, single massive touch target. The whole 5-phase flow collapses to one "I am here. Seal it." button. Background does the real Ed25519 keygen + PGLite `sovereign_identity` insert + covenant signature + first Resin credit with zero decisions. The final screen is giant "The ropes are open. Your vault is sealed to this device." + a big printable QR envelope.
  - **3 Spoons (Derelicte Editorial):** Cinematic framer-motion GenesisFlow with magazine typography, bylines ("Forged in the Calcium Cage • Photographed at 863 Hz"), pull-quotes from the covenant, elegant "Slide to Abdicate" that feels like a couture gesture.
  - **6 Spoons (Magnum Runway Terminal):** Dense emerald CRT + live 3D Posner Lattice / geodesic tetra (reuse or port `PosnerLatticeScene.tsx` / dome tech). While IdentityForge runs the real `@noble` keygen, the four vertices of the tetra (Engineer, Believer, Navigator, Anchor) light up sequentially with real cryptographic events. Private-key bytes rain as matrix code in a subtle canvas. Telemetry HUD shows "LARMOR 863 Hz — KEY SEALED — PGLITE GENESIS COMPLETE — COVENANT SIGNED".

**Real Crypto Steps Performed (connected, not static):**
1. Real Ed25519 keypair (via shared onboarding utils or direct import of @noble/curves — already a dep in the package).
2. Real PGLite initialization with the genesis schema (`sovereign_identity` row for the family member's alias/vertex, role=genesis_pending → mesh_sovereign).
3. Real covenant payload signed with the new private key; signature + payload stored in `genesis_covenants` + dropped into `sync_queue` for CRDT replication to the Delta.
4. First LOVE / Resin credit earned simply by completing the ritual (via resinEngine).
5. Optional final hardware anchor step: offer to register a WebAuthn passkey against the existing `/api/passkey` worker (the "Node Zero" style binding) for the "un-stealable" device soul the syllabus promises.
6. Final "envelope": Use or extend `qr-printable.ts` (or a new `ignition-envelope.ts`) to generate a real, print-ready sovereign QR containing the public key + a deep link that can re-hydrate the identity on another device or trigger the passkey flow. This is the physical VIP pass the family member actually receives.

**Never-Seen-Before Elements:**
- The Spoon Dial is the **ritual director** — cognitive energy literally modulates cryptographic theater.
- The high-fashion ZOLANDERY narrative (Zoolander difficulty names, "really good-looking people", "runway") is the *exact* security UX.
- A 3D Posner Molecule "calcium cage" (the operator's core medical metaphor from the Cognitive Passport) is the live visualization of the key-sealing ceremony.
- One ceremony produces a fully sovereign mesh node (PGLite + Ed25519 + signed covenant + first LOVE credit + optional passkey) that is immediately usable by BONDING, warehouse, sync bridge, etc.
- 1-spoon path is so simple that a 6-year-old on a tablet can complete real sovereign onboarding with one tap.

This directly fulfills the "4 Nodes of the Delta" doctrine in the passport as a 3D + 4-phase living ritual.

---

## UPDATED EXECUTION PLAN (Supersedes Previous Polish-Only Scope)

The previous "static syllabus polish + E2E + deploy" work is now **Phase 0 foundation**. We keep all the ZOLANDERY enhancements and storyline alignment, then layer the live connected ignition on top.

### Phase 0 — Foundation (keep the good work)
- Fix the `animate-in` classes (native Tailwind or plugin).
- 1-spoon white-mode supremacy vs global dark.
- Storyline alignment pass on `syllabus.ts` (Vols I–II now reference PHOS, PGLite, passkey, CRDT, LOVE, 4 Nodes).
- ZOLANDERY 11/10 visual upgrades to SyllabusPortal (magazine grid in 3, CRT in 6, self-referential winks, runway transitions).
- Keep the existing static fake-QR ignition as the "storybook" teaser.

### Phase 1 — Wire the Shared Onboarding Package
- Add workspace dependency or direct import path for `shared-components/onboarding` into p31ca (check `package.json` workspace config).
- Create `src/components/DeltaRunwayIgnition.tsx` (or evolve SyllabusPortal) that:
  - Imports `GenesisFlow`, `useSpoonState`, `forgeIdentity`, `persistIdentity`, etc.
  - Keeps the live Spoon Dial as the mode switch for the entire ceremony.
  - Maps the 4 Genesis phases to the 4 Delta nodes + PHOS phases for visual poetry.
  - During Forge: conditionally renders a 3D calcium-cage visualization only in 6-spoon (or a simplified canvas in 3-spoon, nothing in 1-spoon).
- New Astro page `src/pages/ignite.astro` that mounts the component with `client:load`, same AppShell treatment (suppressEBC for clean runway feel).
- From the syllabus "Initiate Ignition Sequence" button (in Vol 3), either deep-link to `/ignite?alias=...&vertex=...` or mount the ceremony in-place for progressive enhancement.

### Phase 2 — Real Crypto + PGLite + QR Envelope
- In the Forge step, actually call the shared `forgeIdentity` + `persistIdentity` (PGLite genesis schema).
- After covenant, call resinEngine to grant first credit.
- Optional: surface the existing passkey register flow as the "hardware seal" step.
- Final reveal screen: generate a real printable ignition envelope QR (extend `qr-printable.ts` with a new "sovereign-identity" type) that encodes pubkey + recovery payload. The text "Non-Exportable Ed25519 Key Generated. PGLite Schema Hydrated. The ropes are open." now reflects *actual* state.

### Phase 3 — E2E Calibration for Live Ceremony (extended from directive)
- New or augmented `e2e/delta-runway-ignition.spec.ts`.
- Tests must cover:
  - All three spoon modes still render correctly inside the ceremony.
  - 1-spoon path completes with one giant tap and produces PGLite rows (assert via exposed test helper or post-ceremony query).
  - 3/6-spoon paths exercise the real keygen + covenant signature (cleanup after each test).
  - Final QR envelope is generated and contains the real public key.
- The original 4 static tests from the MISSION DIRECTIVE still pass for the syllabus "storybook" mode.

### Phase 4 — Build, Verify, Deploy:dev
- Same heavy `pnpm run build` (pre-verifiers must still pass; the new ceremony must not break any existing sovereignty or style checks).
- `pnpm run deploy:dev`.
- Capture the preview URL.
- Manual smoke on Android Chrome for 1-spoon (kid-tablet friendly) + desktop 3/6-spoon theatrical runs.

### Phase 5 — Closeout & Review
- Same rules: no commit unless asked.
- `suggest /local-review-uncommitted` with two crisp actions (one for the ceremony component, one for the PGLite + crypto wiring).

---

## FILES THAT WILL BE TOUCHED (expanded from previous)

1. `04_SOFTWARE/p31ca/src/pages/ignite.astro` — **new** (the live onboarding route)
2. `04_SOFTWARE/p31ca/src/components/DeltaRunwayIgnition.tsx` (or `IgnitionCeremony.tsx`) — **new or major evolution** of SyllabusPortal logic
3. `04_SOFTWARE/p31ca/src/components/SyllabusPortal.tsx` — add the "Launch Live Ignition" wiring from the Vol 3 button + keep ZOLANDERY
4. `04_SOFTWARE/p31ca/src/data/syllabus.ts` — storyline alignment + update the VIP Pass module description to say "this is now live"
5. `04_SOFTWARE/p31ca/src/utils/ignition-envelope.ts` — **new** (real QR sovereign envelope generator, extending qr-printable)
6. `04_SOFTWARE/p31ca/e2e/delta-runway-ignition.spec.ts` — **new** (live crypto tests)
7. (possible) `04_SOFTWARE/p31ca/package.json` — workspace dep for shared-components/onboarding if not already hoisted
8. Minor: tailwind config or global.css for any ceremony-specific 3D canvas or CRT layers (keep minimal)

The shared `onboarding/` package itself is **not edited** — we consume it as the authoritative CWP-020 primitive.

---

## SUCCESS CRITERIA (Operator Verification)

- `/ignite` (or the in-syllabus live button) performs real Ed25519 + PGLite genesis + signed covenant + first Resin credit for a test alias/vertex.
- The Spoon Dial remains fully functional and changes the ceremony presentation and cognitive load throughout.
- 1-spoon path is one-tap safe for children on Android tablets.
- 6-spoon path contains a live 3D Posner/tetra visualization of the 4 Delta nodes during key sealing.
- Final QR is a real printable sovereign envelope containing the actual public key.
- All existing syllabus ZOLANDERY and 4 static spoon-dial tests still pass.
- `pnpm run build` + full `test:e2e` green.
- Preview URL live. Manual smoke confirms "connected up, not just static".

---

## RISK & MITIGATION (Updated)

- Shared package not yet wired into p31ca Astro/React build → resolve workspace hoisting or use relative import path; test early.
- PGLite in browser during E2E (OPFS/indexedDB) may be flaky in CI → use memory fallback or test-only in-memory PGLite for the spec.
- 3D during 6-spoon (three already in deps) performance on low-end Android → gate the heavy 3D behind `spoonLevel === 6 && !isLowEnd` or provide a beautiful 2D fallback.
- Heavy prebuild verifiers — run them before any ceremony code lands.
- 1-spoon must never feel like "crypto" — it must feel like "I just said yes to the family and my vault is sealed."

---

## FINAL OUTPUT THE OPERATOR WILL RECEIVE

"Deep dive complete. The three real crypto paths and the unused CWP-020 Genesis package with built-in Spoon hook were the missing pieces. 

We have fused them with the high-fashion Spoon-Dial syllabus into the **Delta Runway Ignition Ceremony** — a cognitive-load-adaptive live cryptographic ritual that has never existed before. 

The 'Initiate Ignition Sequence' button now performs real Ed25519 keygen, PGLite sovereign_identity hydration, covenant signature, first LOVE credit, and produces a real printable VIP envelope QR. The Spoon Dial remains the director of the entire ceremony.

4/4 (extended) E2E passing. Preview live at: https://<hash>.p31ca.pages.dev.

ZOLANDERY level: 12/10 — the runway *is* the crypto.

Local review suggested."

---

**This plan is now the complete, creative, connected evolution of the ultimate assignment.**  
It turns the static story into a living, spoon-aware, cryptographically real onboarding runway that directly implements the 4 Nodes of the Delta as a 3D ritual while staying 100 % true to the "really good-looking people who can't tech good" voice and the 1-spoon accessibility mandate.

Ready for the implementation turn. Call `plan_exit` when satisfied. If you want a one-paragraph sample of how the 1-spoon collapsed ceremony would read on the giant button, say the word and I will draft it before we exit planning.