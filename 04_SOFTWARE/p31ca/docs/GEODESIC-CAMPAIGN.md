# GEODESIC campaign — spec location

The **full** specification (progressive tracks, `fireCoachEvent`, K₄ live, operations) lives in the **P31 home** workspace, not in every partial clone of Andromeda:

**`docs/GEODESIC-CAMPAIGN.md`** (repo root: BONDING Soup + `docs/`)

If you only have `andromeda/04_SOFTWARE/p31ca`, read that path from a multi-root checkout or open the deployed source: **`public/geodesic.html`**. Contract pointer: `ground-truth/p31.ground-truth.json` key **`routes.geodesic`**.
