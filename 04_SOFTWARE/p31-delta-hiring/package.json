{
  "name": "p31-delta-hiring",
  "version": "1.0.0",
  "private": true,
  "description": "P31 Delta hiring: role packets, work-sample rubrics, portable proof export",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -p tsconfig.json --noEmit && vite build",
    "preview": "vite preview",
    "verify": "node scripts/verify.mjs",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint src tests",
    "check": "pnpm run verify && pnpm run test && pnpm run lint && pnpm run build"
  },
  "dependencies": {
    "fuse.js": "^7.3.0"
  },
  "devDependencies": {
    "@eslint/js": "^9.15.0",
    "eslint": "^9.15.0",
    "typescript": "~5.6.0",
    "typescript-eslint": "^8.15.0",
    "vite": "^5.4.0",
    "vitest": "^2.1.0"
  }
}
