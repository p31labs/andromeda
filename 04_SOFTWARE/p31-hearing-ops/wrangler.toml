# Cloudflare Pages — Hearing Ops ships to project `p31ca` (custom domain: ops.p31ca.org).
# Custom domains follow the project's *production* branch. Preview uploads do not replace ops.

name = "p31ca"
pages_build_output_dir = "dist"
compatibility_date = "2025-04-01"
