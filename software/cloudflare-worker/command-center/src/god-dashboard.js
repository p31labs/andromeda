/**
 * Primary dashboard HTML (alias for historical import name in index.js).
 * Implementation lives in epcp-dashboard.js.
 */
export { buildEpcpDashboardHtml as buildGodDashboardHtml } from './epcp-dashboard.js';
