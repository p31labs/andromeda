name = "p31-gridiron"
compatibility_date = "2024-01-01"

# Pages configuration
pages_build_output_dir = "dist"

# Custom domain will be configured via Cloudflare dashboard
# Production: gridiron.p31ca.org
