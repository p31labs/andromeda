{
  "name": "p31-andromeda",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*",
    "extensions/*",
    "04_SOFTWARE/packages/*",
    "p31labs/*"
  ],
  "scripts": {
    "typecheck": "tsc --noEmit",
    "eslint": "echo 'ESLint not configured in root - use 04_SOFTWARE for linting'",
    "license-check": "echo 'License check not configured in root - use 04_SOFTWARE for license scanning'",
    "audit": "pnpm -r audit 2>/dev/null || echo 'Audit skipped'"
  },
  "dependencies": {
    "@types/web-bluetooth": "^0.0.21",
    "@upstash/redis": "^1.37.0",
    "@webgpu/types": "^0.1.69"
  }
}
